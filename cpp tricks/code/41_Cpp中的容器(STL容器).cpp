#include "iostream"
#include "vector"


using namespace std;


int main(){
    // vector<int>   容器类的名字
    vector<int> a;  // 容器对象a，a代表一个可变长的数组，数组中的每个元素都是int类型的变量
    vector<double> b; // 容器对象b，b代表一个可变长的数组，数组中的每个元素都是double类型的变量
    return 0;
}
